# Child<P>

`Child<P>` is an abstract base implementation of `IChild<P>`.

It provides a mutable `Parent` property for child objects that need to know the parent object that owns them.

Use it as a base class for simple composite-pattern child objects when no key is needed.
